// Example object
let categories = {
    fruits: ['apple', 'banana'],
    vegetables: ['carrot', 'lettuce'],
    grains: ['rice', 'wheat']
};

// Extract the first key
let category = Object.keys(categories)[0];

console.log(category); // Output: 'fruits'
